{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "system_url": {
      "type": "string",
      "title": "System URL",
      "description": "The base URL of the SAP CDS instance. For example, https://api.litmos.com/v1.svc"
    },
    "oauth_Url": {
      "type": "string",
      "title": "oauth_url",
      "description": "The API Key is passed as a header and used for authenticating the API calls by the SAP CDS system",
      "format": "password"
    },
    "client_id": {
      "type": "string",
      "title": "client_id",
      "description": "The API Key is passed as a header and used for authenticating the API calls by the SAP CDS system",
      "format": "password"
    },
    "client_secret": {
      "type": "string",
      "title": "client_secret",
      "description": "The API Key is passed as a header and used for authenticating the API calls by the SAP CDS system",
      "format": "password"
    },
    "source": {
      "type": "string",
      "title": "Source",
      "description": "Free text description of your app. Usually a company or product name."
    },
    "application_name": {
      "type": "string",
      "title": "Application Name",
      "description": "The name of the Application CR where the REST APIS should be registered for consumption"
    }
  },
  "required": [
    "system_url",
    "oauth_Url",
    "client_id",
    "client_secret"
  ]
}

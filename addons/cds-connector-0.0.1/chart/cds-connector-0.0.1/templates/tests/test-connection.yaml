apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cds-connector-0.0.1.fullname" . }}-test-connection"
  labels:
{{ include "cds-connector-0.0.1.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cds-connector-0.0.1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
